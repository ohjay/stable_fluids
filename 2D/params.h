#ifndef PARAMS_H
#define PARAMS_H

// relevant systemwide parameters should go here
#define NDIM 2
#define SIDE_LEN 50 // for now, all of our grids are square (replaces L)
#define CELLS_PER_SIDE 50 // number of cells in each coordinate (replaces N)
#define NUM_CELLS (CELLS_PER_SIDE * CELLS_PER_SIDE) // total number of cells in each grid

#endif
